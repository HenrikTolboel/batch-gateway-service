package net.example.batchgateway.adapter.input.events;

/*
@Component
public class UserAddedEventListener implements ApplicationListener<UserAddedEvent> {

    private final Logger logger = LoggerFactory.getLogger(this.getClass().getName());
    @Override
    public void onApplicationEvent(final UserAddedEvent event) {

        // handle the event

        logger.info("UserAddedEvent received: {}", event);
    }
}
*/
